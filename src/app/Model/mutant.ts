export class Mutant {
  alias: string;
  name: string;
  powers: string;
  imagePath: string;

  constructor(alias: string, name: string, powers: string, imagePath: string) {
    this.alias = alias
    this.name = name
    this.powers = powers
    this.imagePath = imagePath

  }

}
